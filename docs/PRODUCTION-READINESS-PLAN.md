# FabricXAI — Production Readiness Plan (code workstreams)

**Created:** 2026-08-05, from the re-verification audit (see `docs/DEPLOYMENT-READINESS-AUDIT.md` for finding IDs; new findings N1–N4 and the regression evidence are from the 2026-08-05 verification pass).
**Scope:** everything code-side. **Deployment/ops items are deliberately excluded** — the S3 proxy fix, backup service + pgBackRest wiring + restore rehearsal, compose/Caddy health gating, pgbouncer healthcheck verification, CI deploy job, and `.env.production.example` fixes are tracked separately and remain go-live gates.
**How to use:** phases are ordered by risk; tasks inside a phase are sized to roughly one working session each. Do them in order unless a dependency note says otherwise. Tick tasks here and note the commit, same convention as the audit file.

Legend: 🅑 blocker · 🅗 high · 🅜 medium · `[~]` = mechanism done, adoption ongoing

---

## Phase 0 — Repo hygiene ✅ **complete 2026-08-06**

- [x] **0.1 🅑 Commit the working tree.** `c9be869` — Tracked `src/components/shell/{sidebar,page-shell}.tsx` import **untracked** files (`factory-chip.tsx`, `nav-icons.tsx`, `search/`); a partial commit breaks the build from a clean checkout. Commit the shell redesign + `/factory` route + search as one reviewable slice. (Two lint fixes were needed on the way in: setState-in-effect and `role="combobox"` in `top-bar-search.tsx`.)
  *Verify:* `git status --porcelain` empty; fresh `git clone && pnpm build` succeeds.
- [x] **0.2 🅑 Close the fail-open route gate.** `2a89192` — `src/app/(app)/layout.tsx:81` — `allowed = !item || canSee(...)`: any route without a NAV entry is visible to every role (`/factory` proves it). Added a `factory` NAV entry carrying a new `hiddenFromSidebar` flag (governed + findable, not listed), flipped the default to fail-closed, and extracted the whole decision into `resolveAccess()` so it is asserted as a function rather than by grepping the layout. Route sweep added to `access.test.ts` (red-tested — it names the offending route); `role-gates.integration.test.ts` covers the inverse, that `/factory` still opens for a storekeeper.
  *Verify:* new test fails if a page is added without a NAV entry. ✅ 725 unit · 609 integration.
- [x] **0.3 🅜 Replace the NUL-byte separators (BE-H6).** `464c95f` — `orders/service.ts:173`, `cutting/cutting.ts:119,183`, `quality/quality.ts:468,477` carried a literal NUL in composite map keys, so grep/ripgrep treated the files as binary and skipped them silently (this corrupted two audits). Now `src/lib/keys.ts` — `KEY_SEP` = U+001F **written as an escape**, plus `compositeKey`/`splitKey` — adopted at all five sites. The guard is a vitest sweep in `keys.test.ts` rather than a bespoke CI step, so it fails locally too; red-tested, and it names the offending file.
  *Verify:* `grep -rlaP '\x00' src --include='*.ts'` → empty; `grep -c recordChange src/modules/orders/service.ts` → 5 (was 0); `file(1)` reports UTF-8 text on all three. ✅ 731 unit · 610 integration.
- [x] **0.4 🅜 Seed prod-guard (INFRA-M10, code half).** `0c3ffec` — the only guard was a silent early return in `seedCredential()` (`src/db/seed/core-slice.ts:57`); every other slice still wrote, so a seed aimed at production filled a live factory and reported success. Now `src/db/seed/guard.ts` refuses on `NODE_ENV=production` **or** a non-loopback `DATABASE_URL`/`DIRECT_DATABASE_URL` (compose service names count as non-local — that is what production resolves), overridable with `SEED_FORCE=1`. It runs from `guard-boot.ts` imported **above the slices**, because called from `main()` it landed after `@/lib/env` threw its own unrelated error first. `deploy.md`'s claim that the script already refused is now true.
  *Verify:* `NODE_ENV=production pnpm seed` → exit 1 naming the reason; remote host → exit 1 naming the host; normal local seed → exit 0; `SEED_FORCE=1` → proceeds, warning that seeded passwords stay refused. ✅ 743 unit · 610 integration.
- [x] **0.5 🅜 Sync the trackers (PROC-3 re-drift).** `709f007` — STUBS claimed Bangla on 1 of 12 floor routes with no picker, only `CORE_SLICE` registered, no approve-inbox HTTP surface, and `delivery.appUrl` on localhost — all four false. PROGRESS called store the "first route reading Bangla" and said production infrastructure did not exist. Every changed claim was re-checked against code, not against the audit prose; two survived in a more precise form (cutting/shipment *do* exercise their machines — what is unasserted is the typed 409). Four new rows added for the re-verification findings (N1 action roles, N2 auth-table RLS, inert `MARBIM_ENABLED`, search layering).

---

## Phase 1 — Authorization & tenancy (BLOCKER class — nothing real goes live before this)  ✅ **complete 2026-08-06** (1.3's adoption ratchet continues)

- [x] **1.1 🔑 Role authorization on every server action (finding N1).** `5b92922` — `requireRole` had zero callers; all 16 `modules/*/actions.ts` were session-only, so any member could open the PP gate, issue a PO, open a BTB, confirm ex-factory or open an LC submission. **69 call sites across 16 modules now gate on roles**, derived from the sync handlers (store/quality/sampling/cutting keep exactly theirs), `nav.ts` audiences, and the services that already gate themselves. No action is broader than its module's nav entry. Owner/admin supervisory on every call; an empty role list throws. Reused `errors.forbidden` (already en+bn) instead of adding `errors.role_forbidden`. Payroll's stricter hr+owner rule stays in 10.1's service.
  *Verify:* `action-role-gates.test.ts` sweeps every `actions.ts` and names any ungated export, any `requireCtx`, any empty gate (red-tested); 6 cases in `role-gates.integration.test.ts` prove it against real signed-in sessions. ✅ 755 unit · 616 integration.
  *Owed next:* the gate refuses with a bare `errors.forbidden` and no naming of the operation — plan 2.3 (typed action results + gate copy) is what makes that refusal readable to the person who hit it.
- [x] **1.2 🔑 RLS on the auth tables (finding N2).** `329e52d` — migration `0073_auth_tables_rls`. Deny-all was not available (Better Auth queries these unscoped, so it would have broken every login), so the policies are **scope-conditional**: unscoped behaves as today; under a scope `users`/`profiles` narrow to people sharing the company via `roles`, and `accounts`/`sessions`/`verifications` return nothing. Measured on the app role: scoped sees 8 users with the policy, 26 without.
  *Verify:* `auth-tables-rls.integration.test.ts` (8 cases, both directions incl. the unscoped login path); the suite performs 9 real signups/logins. ✅ 755 unit · 624 integration · `db:check` clean · seed sweep 134 tables at 0 rows.
  *Known limit, stated in the migration header:* this walls off all MODULE code, not the pooled handle — a future unscoped query still sees everything, through the same door Better Auth uses. Closing that means giving auth its own DB role and connection (architecture change, not a policy). Also: signup inserts `profiles` inside a scoped tx and inserts `roles` immediately before it; swapping those two statements breaks signup.
- [~] **1.3 🔑 Second tenancy wall (BE-B1).** `1d2c81c` (mechanism) + `8d97217` (first module) — **mechanism complete, adoption 1 of 23 modules.** `modules/core/scoped.ts` gives `scoped(table, ctx, …)`, structurally typed so a table without `companyId` is a compile error; `eslint-rules/require-tenant-predicate.js` enforces it on an explicit `files:` list in `eslint.config.mjs` — the ratchet. **10.1 workforce fully adopted** (19 where-clauses; two private helpers had to take `ctx`), chosen first as the 🔒 module.
  *Verify:* with RLS disabled on `payroll_runs`, the unscoped query returns another company's run and the scoped one returns nothing — two walls failing independently, which is the whole claim of rule 2. Rule unit-tested incl. must-not-fire cases (caught a real bug: raw `sql` predicates live in a TemplateElement). ✅ 756 unit · 624 integration.
  *Still owed (tracked in STUBS):* commercial (30 sites), finance (20), store (18), then ~380 more. Adding a module = adopt its queries, then add the file to the lint rule's list.
- [x] **1.4 🔑 Action-layer lint scope (BE-H1) + search relocation.** `e922844` — the rule-1 glob covered `src/app/actions/**` (one real file) and missed all 16 `src/modules/*/actions.ts` plus `src/components/**`. Now covers both, and bans importing `@/modules/*/schema` or `drizzle-orm` there too — the shape both real breaches took. Search's six raw cross-module reads moved to their owners' `queries.ts` (each `scoped()`); `shipment/actions.ts`'s dynamic-import drizzle read moved to `shipment/queries.ts`. Search hardened: 3-char minimum shared client/server, `LIMITS.search` 60/min fail-open, `import 'server-only'`.
  *Verify:* red-tested both banned shapes; 6 integration cases incl. a storekeeper's search never querying the buyer desk. ✅ 756 unit · 630 integration.
- [x] **1.5 🔑 Non-superuser-owner CI job (DB-B1).** `4a36033` — new `owner-privileges` job provisions the hardened owner a real deployment uses (NOSUPERUSER, BYPASSRLS, CREATEROLE), migrates, seeds, and runs `scripts/verify-owner-privileges.mjs`, which exercises all twelve SECURITY DEFINER helpers and **refuses to run against a superuser** (where it would pass regardless — how DB-B1 stayed invisible).
  *Found by writing it — three undocumented prerequisites, each failing without mentioning the owner:* `vector` is untrusted so a non-superuser cannot create it (0000's "provisioned by db:migrate alone" is true only for a superuser); the owner needs CREATEROLE; and **`app.pgbouncer_get_auth` reads superuser-only `pg_shadow`, so without `GRANT SELECT ON pg_shadow` the pooler refuses every client** — a total outage from a grant nobody knew to make. `db:setup-roles` now warns with the exact statement; `deploy.md` documents all three.
  *Verify:* exit 0 correctly provisioned · exit 1 with BYPASSRLS removed (owner cannot read `roles` — login broken) · exit 1 as superuser. Proven locally against a real non-superuser owner.
- [x] **1.6 🔑 Auth-rate-limit parity in CI.** `46aa987` — **not** by setting `RATE_LIMIT_ENFORCE=1` on the integration job as written: `lib/auth.ts` already documents why that fails (signup is 3/hr per IP; the suite signs up nine owners from one address). Instead one file runs alone in its own step with the flag, asserting both directions — the first ten sign-ins must NOT be refused, the eleventh must be.
  *Verify:* the step greps for `2 passed` because vitest exits 0 when everything skips and this file skips itself without the flag — guard verified to fail the step when the flag is absent. Test made repeatable after finding it was not: the bucket is keyed by IP, so back-to-back runs shared one 5-minute window. ✅ 631 passed / 2 skipped.
---

## Phase 2 — Money, dates, audit, gates (legal/money correctness)  ✅ **complete 2026-08-06** (2.3 typed results and 2.9 conversion remain as ratchets)

- [x] **2.1 🗝 LC latest-shipment gate (BE-H2).** `05f9b7e` — **implemented as a waivable gate**, not demoted: rule 8 names it a gate, so the code now matches the law. `confirmExFactory` refuses when the credit cannot accept the shipment date (`latest_shipment` or `expiry`); `waiveLcDate` lets commercial/owner accept the breach on the record beforehand — role-gated, ≥10-char reason, audited, refused after departure. Blocked event emitted in its own transaction so the attempt survives the refusal.
  *The tension this resolved:* the final-inspection gate blocks because "the goods have not left yet" while the old LC test said "the container is on a truck, and refusing does not put it back". A gate that makes reality unrecordable is the worse failure, so the waiver is what makes blocking honest. `presentation_window` stays advisory — there the date is fine and the risk is document turnaround.
  *Verify:* 4 integration cases (refusal · trail survives · waiver ships and still reports · waiver refused without role/reason/before-departure). ✅ 756 unit · 634 integration. **Waiver wired into the shipment desk** (`fx/shipment-actions.tsx`) — a gate whose escape hatch no screen can reach is not waivable in practice, and the refusal copy points at that exact button. Found in passing: `waiveFinalInspection` has no action and no UI at all (recorded in STUBS).
- [x] **2.2 🗝 EXP bypass in `openSubmission`.** `de0809b` — `createSubmission` called straight into `openSubmission`, so a bank presentation could be opened against a shipment with no EXP number. Now checked inside that transaction (a gate reading in one tx and writing in another has a gap). Remaining hole named in code: a presentation with no `shipmentId` skips it, which is correct for non-export presentations and is also the way to dodge it. Found en route: the suite opened presentations against `randomUUID()`.
- [~] **2.3 🗝 Typed action results + gate facts (BE-H3).** `4ba1338` — **copy done, typed results owed.** All 21 gate reasons that rendered as raw dotted keys now have English (8 also Bangla — the ones the floor meets), written as what to do next, and a test fails any future gate that ships mute. The other half — returning `{ok:false, error}` so the computed FACTS survive the action boundary — changes the return contract of 69 actions and their callers; left open rather than rushed. The copy interpolates those facts already.
- [x] **2.4 🗝 Audit gaps + enforcing registry (BE-B5).** `523c506` — `createLc` and `openSubmission` now write `recordChange`; commercial registers its four ⚖ tables. `registerAuditedTables` was write-only (zero readers) — a test now reads it and found three registered tables with no audit row. Two are legitimately audited under their parent aggregate; those exemptions are listed with their reason, plus a second test that fails when one goes stale.
- [x] **2.5 🗝 Factory-timezone dates (INFRA-H2).** `5a8b5ea` — `lib/dates.ts` implemented (`factoryToday`/`factoryMonth`/`toFactoryDate`/`startOfFactoryDay`), the five private copies deleted, 27 call sites migrated, and a lint ban on the argument-less `new Date().toISOString().slice(...)`. The ban deliberately does NOT catch UTC arithmetic on an explicit calendar string — that is correct code, and a rule wrong about correct code gets disabled. Tests pin instants, because this bug is invisible 18 hours a day.
- [x] **2.6 🗝 BTB TOCTOU in `issuePo`.** `f9b5f59` — `checkBtbHeadroomIn` runs inside the caller's transaction with `FOR UPDATE` on the master credit. The old call opened a second connection while the first transaction was live — the deadlock shape `saveBreakdownIn` documents — and read headroom outside the tx that wrote the PO.
- [x] **2.7 🗝 Float quantity + lint scope.** `f9b5f59` — `commitStockAdjustment` now uses scaled BigInt (it was float arithmetic on the bonded roll quantity UD reconciliation is computed against). `no-float-money` extended to `src/lib` and `src/components` — the layer that FORMATS money and was never checked; 6 hits, all name-heuristic false positives, each given a stated reason.
- [x] **2.8 🗝 State machines for the raw ⚖ status columns.** `83a086a` — `uds`, `rolls`, `wage_gazettes`, `receivables`, `payables`. Each machine documents what goes wrong on the floor if a move were allowed (a revived UD is fabric drawn against a dead permission; a twice-issued roll exists in two places). 11 cases assert the illegal directions as typed 409s.
- [~] **2.9 🗝 Money type consolidation (BE-M8).** `70097a4` — **ratchet in place, conversion owed.** New local `toMinor`/`fromMinor` copies are banned by a custom rule; the 20 existing files are a shrink-only exemption list. Written as a custom rule after the `no-restricted-syntax` version silently did nothing — flat config REPLACES a rule's options when a later block sets the same name, so it and the date ban cancelled each other out. Found by red-testing, not by a green run.
---

## Phase 3 — Approve inbox & trust-spine verification ✅ **complete 2026-08-06** (every ⚖ mutation funnels through this)

- [x] **3.1 🅗 `approvals/register.ts` + registry entry (BE-B7 fallout).** ✅ 2026-08-06. `pendingTargets: []` and `zodMap: {}` — the boundary is not a desk, and a drafted change to who may approve what is a drafted change to the control. Primer `X.1.0` teaches the queue, per-field confidence (weakest, never the average), distinct-approver counting, aging against the tenant's own window, and that the correction rate is quoted even when unflattering. Seven read tools, no draft tool; `approvals.provenance` redacts to field NAMES so a wage rate cannot reach `chat_turns`. Two findings recorded in STUBS: **nothing refuses self-approval** (`actions.ts` documented a control that never existed), and five of this module's functions had no caller anywhere before these tools.
- [x] **3.2 🅑 Approvals test suite (TEST-B1).** ✅ 2026-08-06. **14 unit + 31 integration**, from zero. Unit: `matchRule` (exported — it is a second copy of core's `resolveRule`, including the surprise that a wildcard outranks an exact rule because priority is the only order), `hoursBetween`, `correctionRate`. Integration: tenancy on `inboxRows`/`draftDetail`/`auditChain`/`inboxCounts`; role routing; **the inbox and `approve()` agreeing from both directions** — the role the queue hides a draft from is refused, the role it offers it to is accepted, on a matched rule and on the fallback; two-approver rules needing two people; aging against the tenant's window. Three defects found and fixed: `approval_rules` wrote **no audit row at all** (the one control-bearing table with no trail, now ⚖ registered with before/after), `upsertApprovalRule` was a bare INSERT behind an upsert name (two active rules for one scope, first-by-priority wins, so a tightened rule could silently not apply — now supersedes to `is_active = false`), and the fixture bug worth remembering: **a user with no `roles` row is invisible to any join under a tenant scope** since migration 0073, which showed up as a real approval with a null approver name. Two more recorded not fixed: the aging job is not idempotent (its comment claimed it was), and `upsertApprovalRule` still has no caller anywhere. UI has no test infrastructure in this repo — 3.4 covers the batch-approve behaviour.
- [x] **3.3 🅗 Drive every registered pending target through `approve()` (TEST-H4).** ✅ 2026-08-06. **All 26 registered targets across 15 modules, 29 cases**, through the shared `proposeApproveCommit` helper (`modules/core/__tests__/support/pending.ts`, which also asserts the invariant half: created `pending`, committed not `awaiting_approvals`, and the draft closed WITH the row). Each case asserts the committed row's *shape*, not just that something was written — the gazette's grade table (a header alone activates cleanly and pays nothing), the BOM's `planned` basis, the carton's derived total, the roll quantity a write-off actually moves, the breakdown's bumped revision, the enquiry's outbox event. A coverage ratchet walks the live registry in both directions, so a new target cannot land untested and a case for a removed one cannot masquerade as coverage. Findings: **`planning/allocations` commits only `scenario_apply`** — a draft using the module's own `allocation` key validates, renders and dies at the click (recorded in STUBS; no producer yet); `finance/payables` has a *second* commit path selected by operation (`pay_payable`) that is now covered; and the `rolls` fixture needs a GRN line and a location, because stock here is the sum of rolls and a roll with no receipt behind it is untraceable on-hand.
- [x] **3.4 🅜 Batch-approve partial failure (new).** ✅ 2026-08-06. **This item's premise was wrong and the bug was worse.** The batch already used `Promise.allSettled`, so it did not abandon the rest on one failure — but it counted every settled promise as an approval, and `approveDraft` returns `awaiting_approvals` when a draft is short of its signatures. Batching ten drafts under a two-approver rule reported **"10 approved" with nothing committed and all ten still in the queue** — precisely the trap `ApproveResult`'s own docstring warns about. Fixed with three real changes: outcomes are classified `committed`/`awaiting`/`failed` and worded as "committed", never "approved"; each failure keeps its own message beside its row title in a dismissable panel (the batch discarded every `reason`, so "3 of 40 failed" named neither which three nor why); and the selection is now cleared *after* the run down to exactly what did not commit, so a retry is one click and can never re-approve a committed row. Also bounded to 4 concurrent — a "select all" of 60 held a transaction each against a PgBouncer pool of 25 and would have stalled every other screen in the factory. Logic extracted to `approve/batch.ts` with **10 unit tests**, since a counting bug in a component with no test infrastructure is a counting bug that comes back.
- [x] **3.5 🅜 Remaining replay/machine tests (TEST-H6/H7 residue).** ✅ 2026-08-06. **+8 integration cases and one real fix.** Sampling's two sync handlers now have replay coverage: a resent `advance_stage` returns `duplicate` and writes one event (and the key lands on the business row, not only in the sync ledger); a *fresh* key advancing to the same stage is refused `stage_not_forward`, which is the guard for a device that cleared its queue. `packingListMachine` gets its illegal-transition cases — the sharp one is that approval cannot be re-run, because approving supersedes the earlier versions and a second pass reports a smaller count as though it were the truth. **BE-M3 was a live bug, not a tidiness item.** `record_feedback` and `recordBuyerVerdict` call the same service; the sync handler carried an idempotency key and the action did not, and feedback rounds are NUMBERED with the latest one being the verdict in force. So a retried submit — dropped response, double tap — wrote round 2 with the same words, and one buyer email became a two-round history. Migration `0075` adds `sample_feedback_rounds.offline_key` with the partial unique index every other offline-keyed table uses; the service checks under the request's row lock and returns the ORIGINAL round, emitting nothing (re-emitting would tell the cutting floor a second time that it may start). The screen supplies a key per composed verdict and rotates it only after one lands, so a retry after a failure reuses it. A case pins that a genuinely new round still numbers 2 — the guard must not swallow a buyer withdrawing an approval.

---

## Phase 4 — Floor pilot completion

- [x] **4.1 🅗 Offline path for `quality/final` and `quality/measurements` (FE-H5 residue).** ✅ 2026-08-06. Both screens now `capture()` through the batch endpoint; two sync handlers registered (`final_inspection`, `measurement_set`, roles `quality`). Migration `0076` adds `offline_key` to both tables — unique on `final_inspections`, and deliberately **not** unique on `measurement_checks`, because one key covers the N pieces of a size. **The data-loss bug was real and its comment claimed the opposite:** `recordMeasuredPieces` looped over `recordMeasurementCheck`, each opening its own transaction, so a bad value on piece 2 left piece 1 committed and piece 3 never attempted — a half-measured size that reads as a completed check on a buyer report. `recordMeasuredSet` now validates every piece before writing any, in one transaction, under the spec's row lock. `inspection_no` would already have stopped a resend, but as a *constraint violation* — and a refused row is remembered as refused, so a replayed batch would have told an inspector their inspection failed on a lot that passed; the key returns the original verdict instead, rebuilt from the snapshot on the row rather than by re-reading the versioned table. The final screen also counts inspections filed locally, because the number is derived from `history.length` and three queued on a dead network would all be `-1`. The verdict is still server-computed — queuing changed when the inspector learns it, not who decides it. Bonus: `getPolicyIn(tx, …)` so the sync handler does not take a second pooled connection while holding one. `store/rolls` and `quality/fabric` closed as by-design in STUBS.
  *Verify:* 6 new integration cases — atomic-or-nothing, a failing piece filed beside passing ones, replay ⇒ `duplicate` with no doubled rows, desk-retry ⇒ original set and no second alarm, queued inspection ⇒ server's verdict, replayed inspection ⇒ verdict not refusal. ✅ 802 unit · 724 integration.
- [x] **4.2 🅗 Bangla for the chrome and the door (new gap).** ✅ 2026-08-06. **+123 catalogue keys, both locales.** Nav labels, section headings and role names now resolve through `ui.nav.*` / `ui.role.*` keyed on the NAV entry's own id — so the sidebar, the top-bar search, the read-only banner, the locked card and MARBIM's scope chip all read in the reader's language. The four `(auth)` pages were already inside a `LocaleProvider`; they now use it (47 keys, including the two refusals the login form shows). `shipment.errors` is at parity — 26 keys, and the packing floor is the screen that raises them. Three judgement calls worth naming: the **conjunction is copy, not punctuation** (Bangla joins roles with ও, and a template literal here produced a sentence in neither language); search matches on the translated label **and** the English, because dropping the English would make the box worse for a bilingual merchandiser to make it work for a Bangla reader; and `lockedAs` stays as data with a matching `ui.nav.locked_*` key, because "Owner dashboard" reads as a heading while "the owner dashboard" reads as English — and Bangla takes no article at all.
  *Verify:* `nav-copy.test.ts` walks the registry and demands both locales for every id — `i18n-ui.test.ts`'s literal scan cannot see `ui.nav.${item.id}` by construction — plus assertions that the English catalogue and the NAV data agree, and that `lockedSubject` says word-for-word what `role-gates.integration.test.ts` asserts. Red-tested: deleting one bn key and renaming one English label fails two cases by name. The Bangla-script check gains a named exemption list (MARBIM, HR) with a staleness guard. ✅ 811 unit · 724 integration.
- [x] **4.3 🅜 Pin the i18n allowlist.** ✅ 2026-08-06. The carve-out was **self-fulfilling**: it was derived as "every refusal key with no Bangla", so adding an untranslated refusal grew the exemption and the suite stayed green — the one number that says how far this work has to go could only rise, and nothing said so. Now enumerated (141 keys) with a four-way ratchet: an unlisted untranslated key fails (11c), a listed-but-now-translated key fails (11d), an entry for a key that no longer exists fails (11e), and — the part that is not a list at all — **every `gates.*` block must be bilingual, with no exemption available** (11f). It asks the catalogue directly, so a new gate cannot be waved through by adding a line. That required translating the remaining **17 gate blocks**, which is the real find here: a blocked gate is the refusal that stops work mid-task (a cutter told the PP sample is not approved, a storekeeper told the UD has no balance), and nine of the seventeen were thrown by floor modules onto floor screens. All 30 are now bilingual. The residue is desk copy only — read at a keyboard, by somebody who can escalate it.
  *Verify:* red-tested three ways — a new English-only refusal fails 11c, a translated key left on the list fails 11d, and an untranslated gate fails 11f **even when listed**. ✅ 815 unit · 724 integration.
- [ ] **4.4 🅗 Tablet responsiveness (FE-H6).** Fixed 232px sidebar, one media query in the whole app, and the new top bar adds a 240px hard floor for search. Collapse the sidebar to icons under ~900px (nav-icons.tsx exists now — use it), let the top-bar grid drop the search column minimum, audit the 13 floor screens at 768×1024 and 1024×768.
- [ ] **4.5 🅜 Refused-row reconciliation report (FE-M6).** A rejected offline GRN is dismiss-only today; the plan's report screen (per-day refused rows with reasons, per handler) closes the "silently discarded work" hole.

---

## Phase 5 — Desk write surfaces (the product can advance records; let it originate them)

Order matters: 5.1 unblocks the flagship demo; 5.6 is one line and stops the shell lying today.

- [ ] **5.1 🅗 Orders & TNA operable (FE-B2 + BE-B6 half).** Create `orders/actions.ts` (`createOrder`, `actualizeMilestone`, `applyRevision`, `setOrderStatus` — the services all exist and are worker-only today); make `MilestoneTimeline` interactive (`fx/tna.tsx:67` `onActualize` has no caller; `orders/[orderId]/page.tsx:111` passes no prop); wire revision upload → diff → approve.
- [ ] **5.2 🅗 Buyers operable (FE-B4).** `moveLeadStage`, `logLeadActivity`, `convertLeadToBuyer` have zero importers. Buyer detail drawer + 2-step convert dialog + stage moves on the pipeline board.
- [ ] **5.3 🅗 RFQ operable (FE-S2).** No `rfq/actions.ts` at all over a complete service (`createRfq`, `draftQuote`, `sendQuote`, `markWon/Lost`). Ship the actions + RFQ detail with quote entry and win/loss.
- [ ] **5.4 🅜 Planning operable (FE-S7).** `planning/actions.ts` over the stranded `allocate`/`moveAllocation`/`setAllocationStatus`/scenario services; drag can come after plain buttons.
- [ ] **5.5 🅗 Wire the 16 orphaned origination actions.** The systematic gap: `openShipment`, `raiseSampleRequest`, `reportMachine`, `raiseCap`, `logAudit`, `saveCertificate`, `logTraining`, `recordQuote`, `compareQuotes`, `updatePoStatus`, `recordGazette`, `makeGazetteActive`, `findSimilarStyles`, plus procurement's missing `createPurchaseRequisition`/`createSupplier` actions and commercial's missing `createLc`/`createUd`. Every root record must be creatable from a screen. One module per session; each new action gets its Phase-1 role check on day one.
- [ ] **5.6 🅗 Stop the read-only banner lying (new).** `canWrite` defaults `true` when `writeRoles` is undefined (`nav.ts:318`); only 2 of 25 items declare it, and Orders affirmatively claims three writing roles over zero write surface. Set `writeRoles: []` on buyers/rfq/planning/orders until their phase ships; require the field (make it non-optional in the NavItem type).
- [ ] **5.7 🅗 `/api/production/*` HTTP routes (BE-B6 half, unblocks TEST-B2).** `outputs` (POST, thin: auth → role → zod → service, `offline_key` passthrough) and `board` (GET) — the exact routes `k6/production_burst.js` already targets.
- [ ] **5.8 🅜 Settings substance (FE-S14).** In order of harm: fix the hardcoded `locale:'en'` (`settings-client.tsx:45` silently writes English on first save — add the control, read the existing value); users/roles matrix with grant/revoke (the invitations flow can stay off); policy override editor; audit-log viewer (10+ modules write `audit_log`, no screen reads it); CSV export.

---

## Phase 6 — MARBIM: make "off" real, then decide "on"

- [ ] **6.1 🅑 A real off-switch (pre-req for any pilot).** `MARBIM_ENABLED` has zero runtime consumers. Consume it: hide `MarbimButton`/`MarbimPanel` (`(app)/layout.tsx:98,118`), drop the nav entry, short-circuit `/marbim`, `/marbim/intake`, `/memory` to a `LockedState`; refuse `readDocument` when `!hasProvider()` so nothing queues into a void; treat a provider-skip as not-success in `recordRun`/`lastSuccessByTask` (or drop `marbim.run_extractions` from `SCHEDULED_TASKS` when off) so job health stops reporting green.
  *Verify:* boot with flag off — no AI surface, no queued extractions, `marbim.run_extractions` not green.
- [ ] **6.2 🅗 Remove the false grounding claims now.** `surface-client.tsx:215` "MARBIM states no number it did not read from a tool" and `:129` mapping *requested* tool calls to `state:'done'` rows with an "N tools" receipt — nothing executes. Honest copy until 6.5 lands.
- [ ] **6.3 🅗 Measured confidence + lint rule (AI-B2 — before any real provider).** Eight modules ship hardcoded `fieldConfidence` literals that drive inbox ranking and the auto-approve floor. Mark them user-draft-class (`fieldConfidence: {}`) or derive from measurement (the good example: `memory/memory.ts:352-363`); add the promised eslint rule (numeric literal inside a `fieldConfidence` object ⇒ error).
- [ ] **6.4 🅗 Real provider (AI-B1).** One vendor SDK behind the provider interface (extract with per-field confidence, generate, embed), selected by env; prod boot assertion when `MARBIM_ENABLED=true` (real provider or exit); versioned `extractorVersion` from prompt semver + model id (AI-H1).
- [ ] **6.5 🅗 Tool execution loop (AI-B3) + the gates it exposes.** Model turn → validate+execute → results back → final turn, iteration cap; `runDraftTool` becomes the only write path. Land **with** it: server-side role filtering of tool packs (AI-H6 — the client caption already promises it), intake role gate (AI-H7), chat/token cost ceilings + `marbim_call_log` (AI-H4), conversation history budget (AI-H3), MARBIM events + extraction notifications (AI-H5).
- [ ] **6.6 🅜 Extraction pipeline hardening.** Outbox→BullMQ instead of the 5-min poller (AI-M4), Redis-counter rate limit (AI-M5), prompt-injection delimiting + documented approval-as-containment (AI-M3), OCR decision (AI-M1).

---

## Phase 7 — Test depth & load verification

- [ ] **7.1 🅗 k6 to a runnable baseline (TEST-B2 + TEST-H5, needs 5.7).** Run `production_burst` against `seed --scale=factory`, automate the zero-lost-rows assertion (it's a comment telling a human to run SQL today), commit the baseline; then `store_grn` and `owner_dashboard`; `mixed_day` (the declared release gate) last, once desks write.
- [ ] **7.2 🅗 Frontend test rig (TEST-H8).** A vitest jsdom project that can collect `.tsx` (both configs are node-env, `.ts`-only today); jsdom tests for `use-offline-queue.ts` and `inbox-client.tsx`; one Playwright golden path (login → store GRN → approve inbox); axe-core on the five floor screens.
- [ ] **7.3 🅜 Coverage + CI reporting (TEST-M10).** coverage-v8 with a ratchet floor, JUnit artifacts; add the missing FE/k6 CI homes.
- [ ] **7.4 🅗 Payroll parallel-run harness.** The quality doc's own regime — one month against the factory's Excel, every net diffed to zero or explained — has never run. Build the diff tool (CSV in → per-worker net comparison vs `computePayrollRun`) so the pilot month can execute it.
- [ ] **7.5 🅜 Health endpoint split, code half (INFRA-M1/M13).** `/api/health` = liveness only (no scheduler 503, no env/exception leakage); `/api/ready` = deps; `/api/health/jobs` behind a token (the prod compose comment already advertises this route — it doesn't exist).

---

## Phase 8 — Process & contract

- [ ] **8.1 🅗 Backfill HANDOFFs for the pilot modules (PROC-1/BE-B7).** `docs/handoffs/` is empty; zero of 23 modules has a FINAL contract, and X.1 shipped despite its own stated precondition. Write them retroactively for the pilot set first — store, cutting, production, quality, sampling, commercial, workforce, approvals — each §5 operations / §6 machines / §7 gates checked against code as it's written (they become the per-module acceptance checklists). Or formally retire the process and promote the briefs; either way, decide.
- [ ] **8.2 🅜 Tracker drift automation.** A small CI check that greps the strongest STUBS claims (file-existence class: "no actions.ts", "handler missing") against the tree so the go/no-go documents can't silently rot again.

---

## Dependency map (why this order)

- Phase 1.1 (roles) blocks every new action in Phase 5 from shipping insecure — do it first; new actions adopt the pattern as they land.
- Phase 2.3 (typed results) is a prerequisite for gate UX in Phases 4–5; 2.5 (dates) touches files Phases 4–5 also touch — land it before them to avoid rebasing pain.
- Phase 3 (approve inbox tests) should precede Phase 6.4+ — a real provider multiplies traffic through exactly that untested funnel.
- Phase 5.7 unblocks 7.1. Phase 6.1 is independent and small — it can be pulled forward any time.
- Phases 0 and 6.1–6.2 are the cheapest honest wins; nothing depends on them being late.
